# Simple XP Teleport

Paper 1.21.8 向け Minecraft プラグインの開発用スケルトンです。

## Build

```bash
./gradlew build
```

生成物は `build/libs/simple-xp-teleport-x.x.x.jar` です。

## Run Development Server

```bash
./gradlew runServer
```

初回起動時に Paper 1.21.8 の開発用サーバーが `run/` 配下に作成されます。
